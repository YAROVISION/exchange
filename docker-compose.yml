version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: exchange-app:latest
    container_name: exchange-web
    ports:
      - "7070:7070"
    env_file:
      - .env
    restart: unless-stopped
