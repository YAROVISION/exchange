# Config for finetuning kronos on BTC daily data
# Конфігурація для тонкого налаштування Kronos на денних даних BTC-USD

data:
  # Шлях до файлу з вашими даними біткоїна
  data_path: "/Users/kostantinkrivula/Desktop/Kronos-master/data/btc_history_daily.csv"
  lookback_window: 400         # Скільки точок в історії дивиться модель (lookback)
  predict_window: 120          # Скільки точок прогнозуємо (для порівняння при навчанні)
  max_context: 512             # Максимальний контекст моделі
  clip: 5.0                    # Обрізання викидів при нормуванні
  
  # Розподіл даних на навчальну та валідаційну вибірки
  train_ratio: 0.9             # 90% для навчання
  val_ratio: 0.1               # 10% для валідації
  test_ratio: 0.0

training:
  tokenizer_epochs: 8         # Кількість епох навчання токенізатора
  basemodel_epochs: 60         # Кількість епох навчання самої моделі-передбачувача
  batch_size: 32               # Розмір батчу
  log_interval: 10
  num_workers: 2
  seed: 42
  
  tokenizer_learning_rate: 0.0002
  predictor_learning_rate: 0.000001
  
  adam_beta1: 0.9
  adam_beta2: 0.95
  adam_weight_decay: 0.1
  accumulation_steps: 1

model_paths:
  # Назви базових моделей на Hugging Face (або локальні шляхи)
  pretrained_tokenizer: "/Users/kostantinkrivula/Desktop/Kronos-master/finetune_csv/finetuned/btc_daily_finetuned/tokenizer/best_model"
  pretrained_predictor: "NeoQuasar/Kronos-small"
  
  # Назва вашого експерименту та папка для збереження донавченої моделі
  exp_name: "btc_daily_finetuned"
  base_path: "/Users/kostantinkrivula/Desktop/Kronos-master/finetune_csv/finetuned/"

  base_save_path: ""
  finetuned_tokenizer: ""
  
  tokenizer_save_name: "tokenizer"
  basemodel_save_name: "basemodel"

experiment:
  name: "kronos_btc_finetune"
  description: "Finetune Kronos on BTC daily data"
  use_comet: false
  train_tokenizer: true        # Чи навчати токенізатор (рекомендовано true)
  train_basemodel: true        # Чи навчати саму модель прогнозу (рекомендовано true)
  skip_existing: false

device:
  use_cuda: false              # На Mac mini використовуємо CPU/MPS прискорення автоматично
  device_id: 0
